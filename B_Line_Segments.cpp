#include <bits/stdc++.h>
using namespace std;
void Muku28(){ios_base::sync_with_stdio(false);cin.tie(NULL);}
#define cin(a,n) for(int i=0;i<n;i++){ cin>>a[i];}
#define nl "\n"
#define dbg(x) cout<<#x<<" = "<<x<<nl;
#define int long long

int N = 1e3 + 5;
int a[N];
int x1, y1;
int n;

bool f(int x, int y, int x1, int y1) {
    if()
}

void solve(int test) {

    cin >> n;
    int x, y;
    cin>>x >> y >> x1 >> y1;

    cin(a, n);
}
int32_t main() {
    Muku28();
    int test=1;
    cin>>test;
    for(int i=1;i<=test;i++) {
        solve(i);
    }
    return 0;
}
